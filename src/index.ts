import {create, build, registerGroups} from "./lib";
import {group, action} from "./decorator";
import {TYPE} from "./declaration";

export {
    create,
    registerGroups,
    build,
    group,
    action,
    TYPE
};
